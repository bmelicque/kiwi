fibonacci :: (n: number) => number {
    if n == 0 { return 0 }
    if n == 1 { return 1 }

    (prev, curr) := (1, 1)

    for _ := 3..=n {
        (prev, curr) = (curr, prev + curr)
    }

    return prev * curr
}

fibonacci(2)